{
  "name": "antigravity-root",
  "version": "1.0.0",
  "description": "Root package.json for Replit compatibility - actual build happens in Dockerfile",
  "private": true,
  "scripts": {
    "postinstall": "echo 'Root install skipped - Dockerfile handles build'",
    "build": "echo 'Build handled by Dockerfile'",
    "start": "echo 'Start handled by Dockerfile'"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
