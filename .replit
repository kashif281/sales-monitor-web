# Replit configuration for Dockerfile deployment
language = "dockerfile"

# Disable automatic package installation
[packager]
ignoredPackages = ["*"]

# Ensure Dockerfile CMD is used, don't override
[deploy]
run = []
